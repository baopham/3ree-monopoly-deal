type Player = {
  id: string,
  username: Username,
  gameId: string,
  actionCounter: number,
  placedCards: PlacedCards,
  game: Game,
  payeeInfo: PayeeInfo,
  save: Function,
  saveAll: Function
}
